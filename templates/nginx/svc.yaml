apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.nginx.name }}-svc"
spec:
  selector:
    app: {{ .Values.nginx.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.nginx.port }}
      targetPort: {{ .Values.nginx.port }}
